# Frontend Configuration
# API URL for backend communication
# For Docker Compose: Use http://localhost:4242 (external access)
# For internal Docker network: Use http://backend:4242 (if needed)
VITE_API_URL=http://localhost:4242

# Port (used by docker-compose.yml)
FRONTEND_PORT=8080
