# URL del backend. Sin esto el frontend no puede cargar el catalogo ni cobrar:
# ensureApiUrl() lanza a proposito en vez de caer a rutas relativas, que en
# produccion apuntarian al propio dominio del frontend.
VITE_API_URL=http://localhost:4242
